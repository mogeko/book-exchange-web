import type { NextPage } from "next";
import { DefaultLayout } from "@/layouts/layout";

const Home: NextPage = () => {
  return (
    <DefaultLayout>
      <></>
    </DefaultLayout>
  );
};

export default Home;
